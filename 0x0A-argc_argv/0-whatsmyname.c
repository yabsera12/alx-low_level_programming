#include <stdio.h>

/**
 * main -  program that Prints the program name, followed by a new line.
 * @argc:  argument.
 * @argv: Array of pointers to the arguments.
 *
 * Return: Always o.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
