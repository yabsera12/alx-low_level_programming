#include "main.h"
#include <stdio.h>

/**
 * mul - multiple of two numbers
 * @a : input type
 * @b : input type
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
